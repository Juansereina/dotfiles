- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.hyper.js:
    ~/.macos:
    ~/.npmrc:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [curl -L git.io/antigen > ~/antigen.zsh]
